# kernel_bounded_buf

Run a [circular buffer](https://en.wikipedia.org/wiki/Circular_buffer) in kernel/user space. 

### Make and run:
#### User space
```c
cd spaces/kernel
make kernel
./buf
```
